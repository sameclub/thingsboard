#+ 新增功能汇总（基于 luming-v1 分支）

以下为本分支相较于上游的新增/改进功能点（含文件定位）。

## 设备详情：遥测图表（Telemetry charts）

- 新增“遥测图表”页签，在设备详情页直接查看各遥测键的实时曲线（默认展示最近 1 小时，1 秒刷新，服务器端不聚合）。
  - 页签入口：
    - src/app/modules/home/pages/device/device-tabs.component.html:35（新增 tab 声明）
  - 组件实现：
    - src/app/modules/home/pages/device/device-telemetry-charts.component.ts（新增）
    - src/app/modules/home/pages/device/device-telemetry-charts.component.html（新增）
    - src/app/modules/home/pages/device/device-telemetry-charts.component.scss（新增）
  - 模块注册：
    - src/app/modules/home/pages/device/device.module.ts:27（import）
    - src/app/modules/home/pages/device/device.module.ts:54（declarations）
  - 文案/国际化：
    - src/assets/locale/locale.constant-en_US.json:1656（device.telemetry-charts）
    - src/assets/locale/locale.constant-zh_CN.json:1559（device.telemetry-charts）
  - 关键行为：
    - 默认时间窗切换为实时 LAST_INTERVAL=1h；`AggregationType.NONE`（源码见 component.ts 构造函数）。
    - 每个遥测 key 自动生成一个 timeseries 小部件，使用 `tb-dashboard` 网格渲染。

## 其他微调/清理（面向产品化）

- 清空应用标题以便品牌定制：
  - src/environments/environment.ts:19、src/environments/environment.prod.ts:19（`appTitle: ''`）
  - src/index.html:17（`<title></title>`）

## 账号与登录

- 禁用客户用户登录（CUSTOMER_USER）：在设置用户会话时拦截并提示“customer.login-disabled”，不允许进入系统。
  - src/app/core/auth/auth.service.ts:540（禁用逻辑开始注释）
  - src/app/core/auth/auth.service.ts:552（提示消息翻译键）

---

如需补充演示数据或扩展该页签（例如多键合并、告警标注、导出等），可继续基于 `DeviceTelemetryChartsComponent` 迭代。
